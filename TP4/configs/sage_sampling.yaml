# TP4/configs/sage_sampling.yaml
seed: 42
device: cuda
lr: 0.01
weight_decay: 5e-4
epochs: 200

sage:
  hidden_dim: 64
  dropout: 0.5

sampling:
  batch_size: 32
  num_neighbors_l1: 10
  num_neighbors_l2: 10